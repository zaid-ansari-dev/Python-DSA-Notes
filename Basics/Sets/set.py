Set = {1, 2, 1, 5}
print(Set)

# no repeated values
# no mutable
# no order
# so no index

s = {}
print(type(s))

# so to create an empty set
sets = set()
print(type(sets))

for value in Set:
    print(value)  # no order maintains
